<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jcca.web.collect.dao.CollectSensorMapper">

    <!-- 查询指定资产的最新传感器数据 -->
    <select id="selectRealTimeData" resultType="com.jcca.web.collect.entity.CollectSensor">
        SELECT *
        FROM COLLECT_SENSOR b
        WHERE b.COLLECT_CODE = (
            SELECT MAX(to_number(COLLECT_CODE)) AS code
            FROM COLLECT_SENSOR
            WHERE ASSET_ID = #{assetId}
        )
    </select>

    <!-- 查询设备内最大的温度 -->
    <select id="selectMaxValue" resultType="com.jcca.web.collect.entity.CollectSensor">
        select max(VALUE) as value,
               ASSET_ID as assetId
        from COLLECT_SENSOR
        where SENSOR_TYPE = 'GAUGE'
        GROUP BY ASSET_ID
    </select>

</mapper>
