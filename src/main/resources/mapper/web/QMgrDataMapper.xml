<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jcca.web.ibmMQ.dao.QMgrDataMapper">

    <!-- 删除过期统计数据 -->
    <delete id="removeExpiredStatistics">
        delete from IBMMQ_QMGR_DATA
        where monitor_id = #{monitorId}
          and captureTime &lt; #{captureTime}
    </delete>

    <!-- 查询最新一条数据 -->
    <select id="queryLastDate" resultType="com.jcca.web.ibmMQ.entity.IBMQMgrData">
        select *
        from (
                 select *
                 from IBMMQ_QMGR_DATA
                 where monitor_id = #{monitorId}
                 order by captureTime desc
             )
        where rownum = 1
    </select>

    <!-- 删除某监控下的所有数据 -->
    <delete id="removeStatistics">
        delete from IBMMQ_QMGR_DATA
        where monitor_id = #{monitorId}
    </delete>

</mapper>
