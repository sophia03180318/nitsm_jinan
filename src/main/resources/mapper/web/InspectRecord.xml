<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jcca.web2.dao.InspectRecordMapper">

    <select id="findCabinetInfo" parameterType="java.lang.String"
            resultType="com.jcca.web2.entity.InspectRecord">
        SELECT CABINET_ID, CABINET_NAME, ASSET_ID, ASSET_NAME, ASSET_IP1, ASSET_IP2
        FROM INSPECT_RECORD
        WHERE CABINET_ID = #{cabinetId}
        <if test="inspectType != null and inspectType != ''">
            <if test="inspectType == 1">
                AND ASSET_STATUS = 1 AND INSPECT_TYPE = 1
            </if>
            <if test="inspectType == 2">
                AND TARGET_STATUS = 1 AND INSPECT_TYPE = 2
            </if>
        </if>
        GROUP BY CABINET_ID, CABINET_NAME, ASSET_ID, ASSET_NAME, ASSET_IP1, ASSET_IP2
        ORDER BY CABINET_NAME
    </select>

    <select id="findLastGroupByOrgId" parameterType="java.lang.String" resultType="com.jcca.web2.entity.InspectRecord">
        SELECT ORG_ID, ORG_NAME FROM INSPECT_RECORD
        <if test="inspectType != null and inspectType != ''">
            <if test="inspectType == 1">
                WHERE INSPECT_TYPE = 1 AND ASSET_STATUS = 1
            </if>
            <if test="inspectType == 2">
                WHERE INSPECT_TYPE = 2 AND TARGET_STATUS = 1
            </if>
        </if>
        GROUP BY ORG_ID, ORG_NAME ORDER BY ORG_ID
    </select>

    <select id="findLastGroupByCabinetId" parameterType="java.lang.String"
            resultType="com.jcca.web2.entity.InspectRecord">
        SELECT CABINET_ID, CABINET_NAME, ORG_ID FROM INSPECT_RECORD
        <if test="inspectType != null and inspectType != ''">
            <if test="inspectType == 1">
                WHERE ASSET_STATUS = 1 AND INSPECT_TYPE = 1
            </if>
            <if test="inspectType == 2">
                WHERE TARGET_STATUS = 1 AND INSPECT_TYPE = 2
            </if>
        </if>
        GROUP BY CABINET_ID, CABINET_NAME, ORG_ID ORDER BY CABINET_NAME
    </select>

    <select id="findCabinetState" parameterType="java.lang.String"
            resultType="com.jcca.web2.entity.InspectRecord">
        SELECT cabinet_id, max(inspect_state) inspect_state
        FROM INSPECT_RECORD
        WHERE CABINET_ID = #{cabinetId}
        <if test="inspectType != null and inspectType != ''">
            <if test="inspectType == 1">
                AND ASSET_STATUS = 1 AND INSPECT_TYPE = 1
            </if>
            <if test="inspectType == 2">
                AND TARGET_STATUS = 1 AND INSPECT_TYPE = 2
            </if>
        </if>
        group by cabinet_id
    </select>

    <select id="findAssetState" parameterType="java.lang.String"
            resultType="com.jcca.web2.entity.InspectRecord">
        SELECT asset_id, inspect_code, max(inspect_state) inspect_state
        FROM INSPECT_RECORD
        WHERE ASSET_ID = #{assetId}
        <if test="inspectType != null and inspectType != ''">
            <if test="inspectType == 1">
                AND ASSET_STATUS = 1 AND INSPECT_TYPE = 1
            </if>
            <if test="inspectType == 2">
                AND TARGET_STATUS = 1 AND INSPECT_TYPE = 2
            </if>
        </if>
        group by asset_id, inspect_code
    </select>

    <select id="findLineStations" parameterType="java.util.Set" resultType="com.jcca.web2.vo.InspectOrgAssetVo">
        SELECT ORG_ID, ORG_NAME FROM INSPECT_RECORD WHERE ORG_ID IN
        <foreach collection="stationids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
        GROUP BY ORG_ID, ORG_NAME ORDER BY ORG_ID
    </select>
</mapper>
