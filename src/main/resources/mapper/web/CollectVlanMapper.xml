<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jcca.web.collect.dao.CollectVlanMapper">

    <!-- 获取指定资产的最新 VLAN 采集数据 -->
    <select id="selectRealTimeData" resultType="com.jcca.web.collect.entity.CollectVlan">
        SELECT *
        FROM COLLECT_VLAN b
        WHERE b.COLLECT_CODE = (
            SELECT MAX(to_number(COLLECT_CODE)) AS code
            FROM COLLECT_VLAN
            WHERE ASSET_ID = #{assetId}
        )
    </select>

</mapper>
