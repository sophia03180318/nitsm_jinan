<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jcca.web.common.dao.DhAlarmMapper">

    <!-- 查询最新告警记录 -->
    <select id="getAlarm" resultType="com.jcca.web.common.entity.Alarm">
        select *
        from DH_ALARM
        where CREATE_TIME = (
            select max(CREATE_TIME)
            from DH_ALARM
        )
    </select>


    <!-- 联表查询告警和设备信息 -->
    <select id="selectAssetAlarm" resultType="com.jcca.web.common.entity.Alarm">
        SELECT
            a.*,
            d.PARENT_ID AS stationId
        FROM DH_ALARM a
                 JOIN dh_device d
                      ON a.DEVICE_ID = d.ID
        WHERE a.DEVICE_ID LIKE '1%'
          AND a.ALARM_FLAG LIKE '%开始%'
        ORDER BY a.OCCURRENCE_TIME DESC
    </select>

</mapper>
