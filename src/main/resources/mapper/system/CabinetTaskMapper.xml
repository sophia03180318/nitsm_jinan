<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jcca.admin.system.dao.CabinetTaskMapper">

    <!-- 查询最新一条任务ID -->
    <select id="getLastOneId" resultType="java.lang.String">
        SELECT id
        FROM CABINET_TASK
        WHERE CREATE_TIME = (
            SELECT MAX(CREATE_TIME)
            FROM CABINET_TASK
        )
    </select>

    <!-- 更新最新一条任务状态 -->
    <update id="setLastStatus" parameterType="int">
        UPDATE CABINET_TASK
        SET STATUS = #{status}
        WHERE CREATE_TIME = (
            SELECT MAX(CREATE_TIME)
            FROM CABINET_TASK
        )
    </update>

    <!-- 根据ID查询状态 -->
    <select id="getStatusById" parameterType="java.lang.String" resultType="int">
        SELECT STATUS
        FROM CABINET_TASK
        WHERE ID = #{id}
    </select>

</mapper>
